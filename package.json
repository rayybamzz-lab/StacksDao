{
  "name": "stacks-dao-scripts",
  "version": "1.0.0",
  "description": "Maintenance and testing scripts for StacksDao",
  "dependencies": {
    "@stacks/network": "^7.3.1",
    "@stacks/transactions": "^7.3.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "clarinet:check": "clarinet check",
    "clarinet:test": "clarinet test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^6.0.2"
  },
  "engines": {
    "node": ">=18"
  }
}
